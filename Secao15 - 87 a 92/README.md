###ARGUMENTS
* arguments --> array like, parecido com um array, funciona como um array, mas não é um array;
* Aparece implicitamente dentro de funcões;
  function exemplo(arg1, arg2){
    return arguments;
  }

  exemplo(1, 2);
  RESULTADO: [1, 2];